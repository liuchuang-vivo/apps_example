export component MainWindow inherits Window {
    title: "Slint in Fly Example";
    width: 320px;
    height: 480px;

    Rectangle {
        width: parent.width;
        height: parent.height;
        background: #FFFFFF;
    }
}
