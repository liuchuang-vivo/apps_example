// Wi-Fi scan UI integrated into the launcher.
// Keep this compatible with SoftwareRenderer::render_by_line: no Path items.

import { AppPage } from "../components/app-page.slint";
import { Theme } from "../components/theme.slint";

export struct WifiNetwork {
    ssid: string,
    detail: string,
    signal-text: string,
    strength: int,
    secure: bool,
}

export component WifiScanPage inherits AppPage {
    title: "无线扫描";
    subtitle: "ESP32-C6  /  2.4 GHZ 网络";
    accent: #4bd6a2;
    action-text: scanning ? "扫描中..." : "重新扫描";
    action-disabled: scanning;

    in property <[WifiNetwork]> networks;
    in-out property <bool> scanning: false;
    in-out property <int> result-count: 0;
    in-out property <string> status-text: "正在准备无线扫描";
    callback scan-requested();

    action-clicked => { root.scan-requested(); }

    // Status card
    Rectangle {
        x: 16px;
        y: 98px;
        width: 448px;
        height: 50px;
        background: Theme.card-background;
        border-width: 1px;
        border-color: Theme.border-color;
        border-radius: 9px;

        Rectangle {
            x: 13px;
            y: 12px;
            width: 26px;
            height: 26px;
            background: root.scanning ? #433c22 : root.result-count > 0 ? #173c32 : Theme.raised-background;
            border-radius: 13px;

            Text {
                width: parent.width;
                height: parent.height;
                text: root.scanning ? "..." : root.result-count;
                color: root.scanning ? #f0c95f : root.result-count > 0 ? #65e0b3 : #82969c;
                font-size: 10px;
                font-weight: 800;
                horizontal-alignment: center;
                vertical-alignment: center;
            }
        }

        Text {
            x: 51px;
            y: 7px;
            width: 376px;
            height: 20px;
            text: root.scanning ? "正在搜索附近接入点" : root.result-count > 0 ? "附近接入点" : "暂无扫描结果";
            color: Theme.primary-text;
            font-size: 11px;
            font-weight: 700;
            vertical-alignment: center;
            overflow: elide;
        }

        Text {
            x: 51px;
            y: 26px;
            width: 376px;
            height: 17px;
            text: root.status-text;
            color: root.scanning ? #d5b95e : Theme.muted-text;
            font-size: 8px;
            vertical-alignment: center;
            overflow: elide;
        }
    }

    if root.result-count == 0: Rectangle {
        x: 16px;
        y: 160px;
        width: 448px;
        height: 250px;
        background: Theme.card-background;
        border-width: 1px;
        border-color: Theme.border-color;
        border-radius: 11px;

        Text {
            x: 0;
            y: 70px;
            width: parent.width;
            height: 50px;
            text: root.scanning ? "..." : "WI-FI";
            color: root.scanning ? #d5b95e : Theme.muted-text;
            font-size: root.scanning ? 30px : 25px;
            font-weight: 800;
            horizontal-alignment: center;
            vertical-alignment: center;
        }

        Text {
            x: 30px;
            y: 127px;
            width: 388px;
            height: 24px;
            text: root.scanning ? "扫描信道 1 至 13" : "点击重新扫描搜索网络";
            color: Theme.secondary-text;
            font-size: 10px;
            font-weight: 600;
            horizontal-alignment: center;
            vertical-alignment: center;
        }

        Text {
            x: 30px;
            y: 156px;
            width: 388px;
            height: 20px;
            text: "SSID、信号、信道和安全信息将显示在此处";
            color: Theme.muted-text;
            font-size: 8px;
            horizontal-alignment: center;
            vertical-alignment: center;
        }
    }

    for network[index] in root.networks: Rectangle {
        x: 16px;
        y: 158px + index * 45px;
        width: 448px;
        height: 39px;
        background: Theme.card-background;
        border-width: 1px;
        border-color: Theme.border-color;
        border-radius: 7px;

        Rectangle {
            x: 10px;
            y: 8px;
            width: 23px;
            height: 23px;
            background: Theme.raised-background;
            border-radius: 12px;

            Text {
                width: parent.width;
                height: parent.height;
                text: index + 1;
                color: #62d4ad;
                font-size: 8px;
                font-weight: 800;
                horizontal-alignment: center;
                vertical-alignment: center;
            }
        }

        Text {
            x: 43px;
            y: 3px;
            width: 238px;
            height: 19px;
            text: network.ssid;
            color: Theme.primary-text;
            font-size: 11px;
            font-weight: 700;
            vertical-alignment: center;
            overflow: elide;
        }

        Text {
            x: 43px;
            y: 20px;
            width: 238px;
            height: 15px;
            text: network.detail;
            color: network.secure ? Theme.secondary-text : #a1832f;
            font-size: 8px;
            vertical-alignment: center;
            overflow: elide;
        }

        Rectangle {
            x: 302px;
            y: 24px;
            width: 4px;
            height: 5px;
            background: network.strength >= 1 ? Theme.accent-color : Theme.divider-color;
        }
        Rectangle {
            x: 309px;
            y: 19px;
            width: 4px;
            height: 10px;
            background: network.strength >= 2 ? Theme.accent-color : Theme.divider-color;
        }
        Rectangle {
            x: 316px;
            y: 14px;
            width: 4px;
            height: 15px;
            background: network.strength >= 3 ? Theme.accent-color : Theme.divider-color;
        }
        Rectangle {
            x: 323px;
            y: 9px;
            width: 4px;
            height: 20px;
            background: network.strength >= 4 ? Theme.accent-color : Theme.divider-color;
        }

        Text {
            x: 338px;
            y: 0;
            width: 98px;
            height: parent.height;
            text: network.signal-text;
            color: network.strength >= 3 ? #6fe0b8 : network.strength == 2 ? #d5b95e : #d07d72;
            font-size: 9px;
            font-weight: 700;
            horizontal-alignment: right;
            vertical-alignment: center;
        }
    }

    // Bottom status bar
    Rectangle {
        x: 0;
        y: 436px;
        width: parent.width;
        height: 44px;
        background: Theme.surface-background;

        Rectangle {
            x: 20px;
            y: 14px;
            width: 5px;
            height: 16px;
            background: root.scanning ? #d5b95e : #4bd6a2;
            border-radius: 2px;
        }

        Text {
            x: 36px;
            y: 7px;
            width: 424px;
            height: 30px;
            text: root.result-count > 6 ? "显示最强 6 个  /  自动刷新: 15 秒" : "信号排序  /  自动刷新: 15 秒";
            color: Theme.muted-text;
            font-size: 8px;
            font-weight: 600;
            vertical-alignment: center;
        }
    }
}
