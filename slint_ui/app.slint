// Blind-box touch demo for the 480x480 CO5300 AMOLED panel.
// Keep this compatible with SoftwareRenderer::render_by_line: no Path items.

component BlindBox inherits Rectangle {
    in property <string> title;
    in property <string> description;
    in property <string> mark;
    in property <color> accent;
    in property <bool> opened: false;
    in property <bool> dark-mode: true;
    callback reveal();

    width: 136px;
    height: 104px;
    border-width: root.opened ? 2px : 1px;
    border-color: root.opened ? root.accent : root.dark-mode ? #36464d : #bdcac8;
    border-radius: 11px;
    background: root.opened ? root.dark-mode ? #17252a : #e7f1ee : touch-area.pressed ? root.dark-mode ? #21343b : #dce8e5 : root.dark-mode ? #121b20 : #ffffff;

    // A small accent strip makes each unopened box distinguishable while the
    // large question mark keeps the interaction obvious on a small panel.
    Rectangle {
        x: 10px;
        y: 10px;
        width: 5px;
        height: root.opened ? 80px : 20px;
        background: root.accent;
        border-radius: 2px;
    }

    Text {
        x: 24px;
        y: 8px;
        width: 100px;
        height: 34px;
        text: root.opened ? root.mark : "?";
        color: root.opened ? root.accent : root.dark-mode ? #dbe7e7 : #344348;
        font-size: root.opened ? 19px : 28px;
        font-weight: 800;
        horizontal-alignment: center;
        vertical-alignment: center;
    }

    Text {
        x: 20px;
        y: 46px;
        width: 108px;
        height: 17px;
        text: root.opened ? root.title : "MYSTERY BOX";
        color: root.opened ? root.dark-mode ? #f0f5f3 : #192529 : root.dark-mode ? #b9c7c8 : #35464a;
        font-size: 10px;
        font-weight: 700;
        horizontal-alignment: center;
        vertical-alignment: center;
        overflow: elide;
    }

    Text {
        x: 18px;
        y: 68px;
        width: 112px;
        height: 18px;
        text: root.opened ? root.description : "TAP TO OPEN";
        color: root.opened ? root.dark-mode ? #93a6a8 : #5d7073 : root.dark-mode ? #6c7e82 : #74868a;
        font-size: 8px;
        font-weight: 600;
        horizontal-alignment: center;
        vertical-alignment: center;
        overflow: elide;
    }

    touch-area := TouchArea {
        pointer-event(event) => {
            if event.kind == PointerEventKind.down {
                root.reveal();
            }
        }
    }
}

export component MainWindow inherits Window {
    width: 480px;
    height: 480px;
    background: root.dark-mode ? #0b1115 : #edf3f1;
    default-font-size: 11px;

    property <bool> dark-mode: true;
    property <int> opened-count: 0;
    property <string> last-reward: "Choose a box to begin";
    property <bool> box1-opened: false;
    property <bool> box2-opened: false;
    property <bool> box3-opened: false;
    property <bool> box4-opened: false;
    property <bool> box5-opened: false;
    property <bool> box6-opened: false;
    property <bool> box7-opened: false;
    property <bool> box8-opened: false;
    property <bool> box9-opened: false;
    callback reset-requested(bool);

    Rectangle {
        x: 0;
        y: 0;
        width: parent.width;
        height: 76px;
        background: root.dark-mode ? #111b20 : #ffffff;

        Text {
            x: 22px;
            y: 10px;
            width: 250px;
            height: 25px;
            text: "LUCKY GRID";
            color: root.dark-mode ? #edf4f1 : #172328;
            font-size: 22px;
            font-weight: 800;
            vertical-alignment: center;
        }

        Text {
            x: 24px;
            y: 42px;
            width: 244px;
            height: 17px;
            text: "OPEN 9 BOXES / FIND 9 SURPRISES";
            color: root.dark-mode ? #728489 : #65777b;
            font-size: 9px;
            font-weight: 700;
            vertical-alignment: center;
        }

        Rectangle {
            x: 280px;
            y: 14px;
            width: 80px;
            height: 46px;
            background: theme-area.pressed ? root.dark-mode ? #31464d : #cadbd7 : root.dark-mode ? #1d3037 : #e1ebe8;
            border-width: 1px;
            border-color: root.dark-mode ? #39505a : #bdcfca;
            border-radius: 8px;

            Text {
                x: 0px;
                y: 4px;
                width: parent.width;
                height: 15px;
                text: "THEME";
                color: root.dark-mode ? #7e999c : #6b7f7e;
                font-size: 8px;
                font-weight: 700;
                horizontal-alignment: center;
                vertical-alignment: center;
            }

            Text {
                x: 0px;
                y: 18px;
                width: parent.width;
                height: 23px;
                text: root.dark-mode ? "DARK" : "LIGHT";
                color: root.dark-mode ? #74daba : #28765f;
                font-size: 11px;
                font-weight: 800;
                horizontal-alignment: center;
                vertical-alignment: center;
            }

            theme-area := TouchArea {
                pointer-event(event) => {
                    if event.kind == PointerEventKind.down {
                        root.dark-mode = !root.dark-mode;
                    }
                }
            }
        }

        Rectangle {
            x: 374px;
            y: 14px;
            width: 82px;
            height: 46px;
            background: root.dark-mode ? #1d3037 : #e1ebe8;
            border-radius: 8px;

            Text {
                x: 0px;
                y: 4px;
                width: parent.width;
                height: 15px;
                text: "OPENED";
                color: root.dark-mode ? #7e999c : #6b7f7e;
                font-size: 8px;
                font-weight: 700;
                horizontal-alignment: center;
                vertical-alignment: center;
            }

            Text {
                x: 0px;
                y: 18px;
                width: parent.width;
                height: 23px;
                text: root.opened-count + "/9";
                color: root.dark-mode ? #6fe0bd : #28765f;
                font-size: 17px;
                font-weight: 800;
                horizontal-alignment: center;
                vertical-alignment: center;
            }
        }
    }

    Rectangle {
        x: 0;
        y: 75px;
        width: parent.width;
        height: 1px;
        background: root.dark-mode ? #2a3a40 : #d3dfdc;
    }

    BlindBox {
        x: 24px;
        y: 88px;
        title: "STAR CACHE";
        description: "A tiny constellation";
        mark: "STAR";
        accent: #f0bd62;
        opened: root.box1-opened;
        dark-mode: root.dark-mode;
        reveal => {
            if !root.box1-opened {
                root.box1-opened = true;
                root.opened-count += 1;
                root.last-reward = "STAR CACHE - a tiny constellation";
            }
        }
    }

    BlindBox {
        x: 172px;
        y: 88px;
        title: "PIXEL WIZARD";
        description: "CO5300 color spell";
        mark: "PIXEL";
        accent: #6fb6f2;
        opened: root.box2-opened;
        dark-mode: root.dark-mode;
        reveal => {
            if !root.box2-opened {
                root.box2-opened = true;
                root.opened-count += 1;
                root.last-reward = "PIXEL WIZARD - CO5300 color spell";
            }
        }
    }

    BlindBox {
        x: 320px;
        y: 88px;
        title: "COFFEE DROP";
        description: "Energy refill unlocked";
        mark: "COFFEE";
        accent: #da966c;
        opened: root.box3-opened;
        dark-mode: root.dark-mode;
        reveal => {
            if !root.box3-opened {
                root.box3-opened = true;
                root.opened-count += 1;
                root.last-reward = "COFFEE DROP - energy refill unlocked";
            }
        }
    }

    BlindBox {
        x: 24px;
        y: 198px;
        title: "BOLT PASS";
        description: "Fast lane for today";
        mark: "BOLT";
        accent: #e8df70;
        opened: root.box4-opened;
        dark-mode: root.dark-mode;
        reveal => {
            if !root.box4-opened {
                root.box4-opened = true;
                root.opened-count += 1;
                root.last-reward = "BOLT PASS - fast lane for today";
            }
        }
    }

    BlindBox {
        x: 172px;
        y: 198px;
        title: "LUCKY GEM";
        description: "Keep this one close";
        mark: "GEM";
        accent: #cf8ff2;
        opened: root.box5-opened;
        dark-mode: root.dark-mode;
        reveal => {
            if !root.box5-opened {
                root.box5-opened = true;
                root.opened-count += 1;
                root.last-reward = "LUCKY GEM - keep this one close";
            }
        }
    }

    BlindBox {
        x: 320px;
        y: 198px;
        title: "MOON MODE";
        description: "A calm night bonus";
        mark: "MOON";
        accent: #9ea8f4;
        opened: root.box6-opened;
        dark-mode: root.dark-mode;
        reveal => {
            if !root.box6-opened {
                root.box6-opened = true;
                root.opened-count += 1;
                root.last-reward = "MOON MODE - a calm night bonus";
            }
        }
    }

    BlindBox {
        x: 24px;
        y: 308px;
        title: "WAVE TOKEN";
        description: "Ride the blue wave";
        mark: "WAVE";
        accent: #64d1d8;
        opened: root.box7-opened;
        dark-mode: root.dark-mode;
        reveal => {
            if !root.box7-opened {
                root.box7-opened = true;
                root.opened-count += 1;
                root.last-reward = "WAVE TOKEN - ride the blue wave";
            }
        }
    }

    BlindBox {
        x: 172px;
        y: 308px;
        title: "HEART NOTE";
        description: "A warm little wish";
        mark: "HEART";
        accent: #f08091;
        opened: root.box8-opened;
        dark-mode: root.dark-mode;
        reveal => {
            if !root.box8-opened {
                root.box8-opened = true;
                root.opened-count += 1;
                root.last-reward = "HEART NOTE - a warm little wish";
            }
        }
    }

    BlindBox {
        x: 320px;
        y: 308px;
        title: "SECRET BONUS";
        description: "The final surprise";
        mark: "BONUS";
        accent: #71e0a9;
        opened: root.box9-opened;
        dark-mode: root.dark-mode;
        reveal => {
            if !root.box9-opened {
                root.box9-opened = true;
                root.opened-count += 1;
                root.last-reward = "SECRET BONUS - the final surprise";
            }
        }
    }

    Rectangle {
        x: 0;
        y: 424px;
        width: parent.width;
        height: 56px;
        background: root.dark-mode ? #111b20 : #ffffff;

        Rectangle {
            x: 24px;
            y: 13px;
            width: 5px;
            height: 30px;
            background: root.opened-count == 9 ? #6fe0bd : #f0bd62;
            border-radius: 2px;
        }

        Text {
            x: 40px;
            y: 8px;
            width: 320px;
            height: 18px;
            text: root.opened-count == 9 ? "ALL REVEALED - JACKPOT!" : root.last-reward;
            color: root.dark-mode ? #edf4f1 : #172328;
            font-size: 10px;
            font-weight: 700;
            vertical-alignment: center;
            overflow: elide;
        }

        Text {
            x: 40px;
            y: 28px;
            width: 320px;
            height: 15px;
            text: root.opened-count == 0 ? "Tap any box to reveal its reward" : "Tap unopened boxes to keep exploring";
            color: root.dark-mode ? #778a8d : #65777b;
            font-size: 8px;
            vertical-alignment: center;
            overflow: elide;
        }

        Rectangle {
            x: 394px;
            y: 12px;
            width: 62px;
            height: 32px;
            background: reset-area.pressed ? root.dark-mode ? #31464d : #cadbd7 : root.dark-mode ? #203239 : #e1ebe8;
            border-width: 1px;
            border-color: root.dark-mode ? #39505a : #bdcfca;
            border-radius: 7px;

            Text {
                width: parent.width;
                height: parent.height;
                text: "RESET";
                color: root.dark-mode ? #9db1b2 : #49605f;
                font-size: 8px;
                font-weight: 800;
                horizontal-alignment: center;
                vertical-alignment: center;
            }

            reset-area := TouchArea {
                pointer-event(event) => {
                    if event.kind == PointerEventKind.down {
                        root.reset-requested(root.opened-count > 0);
                        root.box1-opened = false;
                        root.box2-opened = false;
                        root.box3-opened = false;
                        root.box4-opened = false;
                        root.box5-opened = false;
                        root.box6-opened = false;
                        root.box7-opened = false;
                        root.box8-opened = false;
                        root.box9-opened = false;
                        root.opened-count = 0;
                        root.last-reward = "Choose a box to begin";
                    }
                }
            }
        }
    }
}
